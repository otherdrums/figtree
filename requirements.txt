# PDGA v2 — Fact-Centric Architecture
# Tested on: Python 3.12, CUDA 13.0, Quadro T1000
# Install: pip install -r requirements.txt

# Core ML stack
torch>=2.0
transformers>=4.40
numpy>=1.24
safetensors>=0.4

# 4-bit quantization (required for unsloth/Qwen3-4B-bnb-4bit)
bitsandbytes>=0.49.0
accelerate>=0.20.0

# Tokenizers
sentencepiece>=0.2.0
protobuf>=3.20.0

# CLI and formatting
rich>=13.0

# Development (optional)
# pytest>=7.0
# ruff>=0.1.0
